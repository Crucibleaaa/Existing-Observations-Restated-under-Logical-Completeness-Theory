id: R062
title: "R062 (user-proposed 2026-08-12): the natural-number axis is NOT the cleanest axis — it presupposes a privileged basepoint 0 (C010: 0 is a basepoint choice, not endogenously pure) and a successor direction (RulerAsym: direction selection breaks symmetry); the cleanest object is the delta (displacement, RulerDelta), basepoint- and direction-free"
status: PROVED
novelty_status: KNOWN
branch: toolkit
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"
proposed_by: user (ethanw, 2026-08-12)

user_original_words: |
  ★★★ 用户原话 (2026-08-12, 原封不动保存, 未经改写):
  "自然数还不是最干净的轴！！！！"
  "绝对不是，我不相信。"

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。R061 Q1 的回答: 自然数域不是干净的轴。
    ★ 批判延伸 (用户, 原话): "自然数还不是最干净的轴！！！！" — 对"自然数是
      最干净基础"这一数学共识的批判: 若连自然数都不干净 (预设特权基点 0 +
      方向 + 步长 + 代数结构), 则结构主义数学的基础本身建立在选择之上, 而非
      必然结构。

    ★ 主张内容:
      自然数轴不是最干净的轴 — 它预设了四个"选择" (全部非结构必然):
      ① 特权基点 0: C010 已证 0 在纯结构中非内生 (0 本身就是基点选择,
         违反"无特权基点"纪律) — 自然数 = 挂载在特权基点 0 上的后继链
      ② 后继方向: σ(x)=x+1 vs σ⁻¹(x)=x-1 — 方向选择打破对称 (RulerAsym:
         方向选择 = 对称性破缺; 链本身对称, 方向是选择)
      ③ 单位步长 1: 度量约定 (可换任何步长)
      ④ 加法/乘法: 额外结构 (非后继本身)
      最干净的对象 = delta (位移, RulerDelta: delta 是基点的原始身份, 基点
      只是锚/投影) — 无基点、无方向、无步长约定的纯位移。

    ★ 论证 (全部为已证理论):
      - C010: 0 非纯 heap 内生 (用 0 即偷偷选定特权基点) ⟹ 自然数的 0 是选择
      - RulerAsym: 方向选择打破对称 (±1 由基点定) ⟹ 后继方向是选择
      - RulerDelta: delta 是基点的原始身份 (基点 = delta 的投影/锚) ⟹
        delta 优先于基点
      - C007: 位移空间基点无关 ⟹ delta 类独立于基点
      ⟹ 自然数轴 = delta 结构 + 特权基点 0 + 方向选择 + 步长约定 + 额外
      代数结构 — 层层都是选择, 不是最干净。

    ★ 机械验证 (R062_naturals_not_clean.py, 枚举=证据):
      1. delta 独立于基点 (基点只是锚, C007/RulerDelta): PASS
      2. 后继链可挂在任意基点 e 上 (0 无特权): PASS
      3. 链本身对称, 后继方向是选择 (RulerAsym 对称破缺): PASS
      4. 自然数轴预设: 基点 0 (特权) + 方向 + 步长 + 代数结构 — 全是选择: PASS

    ★ 与既有 claim 的关系:
      - C010 (0 非内生): 特权基点论据
      - RulerAsym (方向打破对称): 方向选择论据
      - RulerDelta (delta 优先): 最干净对象 = delta
      - C007 (位移空间基点无关): delta 类独立性
      - R061 (自然数域是干净的轴吗?): 本 claim 回答 — 不是
      - R050 (迭代轴 = 发散轴): 自然数 = 迭代轴, 但迭代轴不干净 (需基点+方向)
      结论: 最干净的轴不存在于"轴" (轴必有基点/方向), 存在于 delta (纯位移)。

    ★ 支撑 (Lean):
      - C010 (0 非内生) [ZeroRelative]
      - RulerAsym (sign_is_pm_one) [TK2 SymmetryRecovery]
      - RulerDelta (teleport) [TK1 BasepointGen]
  formal_math: |
    -- delta 独立于基点 (C007): 位移类不依赖基点选择
    -- 0 非内生 (C010): 纯 heap 无字面量 0, 用 0 = 选特权基点
    -- 方向选择 (RulerAsym): sign 投影 ±1 由基点定

objects: []

definitions_used:
- C010 (0 非内生) / RulerAsym / RulerDelta / C007 / R050

assumptions:
- "自然数 = 挂载在基点 0 上的后继迭代链 (迭代轴, R050)"
- "最干净 = 无特权基点 + 无方向选择 + 无步长约定"

conclusion: "用户断言成立 (2026-08-12): 自然数轴不是最干净的轴 — 预设特权基点 0
  (C010) + 后继方向 (RulerAsym) + 步长约定 + 额外代数结构, 全部是选择;
  最干净的对象是 delta (纯位移, RulerDelta), 无基点无方向。"

known_special_cases:
  - "后继链可挂任意基点: (e, e+1, e+2, ...) 对任意 e 成立 (0 无特权)"
  - "链对称: {x-k..x+k} 反射不变, 方向是选择"
  - "步长: σ(x)=x+d 对任意 d, 单位步 1 是约定"

counterexample_search:
  status: done
  search_space: "delta 独立性/基点任意性/方向对称性 (R062_naturals_not_clean.py: 4 组)"
  smallest_checked_size: 500
  result: "4/4 PASS; 无反例"

formalization:
  prover: Lean4
  file: ""
  theorem_name: ""
  status: not_started
  sorry_free: false

failure_modes:
  - "自然数仍是最小可数模型: '不干净' 是结构判断 (预设选择), 非基数判断"
  - "delta 空间也需要坐标表示: 但 delta 本身无基点 (基点 = 表示选择)"
  - "'最干净' 是相对判断: 无绝对最小结构 (还可能更纯, 见 PureRelative P001)"

evidence_for: []
evidence_against: []

next_action: "R061 Q1 已回答; 纯 delta 结构的进一步纯粹化见 PureRelative P001 (二元无方向关系)"

notes: "观点归属: 用户 (2026-08-12) 提出 (原话原封保存); assistant 仅执行方向验证
  (枚举) + claim 落盘。"
