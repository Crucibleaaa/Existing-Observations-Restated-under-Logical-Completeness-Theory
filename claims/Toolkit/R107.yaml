id: R107
title: "R107 (user-proposed 2026-08-12): prime-number reference points are the natural implementation of number sense — prime gaps grow smoothly (logarithmic, π(x)~x/ln x) giving NATIVE 'small-exact, large-fuzzy' (小数精确大数模糊) matching the ANS distance effect; equidistant bases give constant precision (mismatch), binary gives stepwise exponential (R106 narrow); among reference systems only primes match the number-sense precision curve"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
branch: toolkit
proposed_by: user (ethanw, 2026-08-12)
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"

user_original_words: |
  ★★★ 用户原话 (2026-08-12, 原封不动保存, 未经改写):
  "不是而，这反而意味着素数更好啊也体现出了小数精确，大数模糊的特征。"
  "好，记录到claim里，然后用我们找到的零点，重新观测这个素数定理的密度递减。"

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。素数参照 = 数感天然实现。

    ★ 主张内容:
      素数做参照更好 — 天然体现"小数精确, 大数模糊":
      素数密度递减 (π(x) ~ x/ln x) = 天然对数分布
      数感精度曲线 (ANS) 与素数间距曲线同构

    ★ 验证 (素数间距平滑增长):
      [2,50) 间距 3.4 | [50,100) 4.8 | [100,200) 5.2 | [200,500) ~6.0
      ⟹ 小数区间参照密集 (精确), 大数区间参照稀疏 (模糊)

    ★ 三种参照精度曲线对比:
      | 参照 | 精度曲线 | 匹配数感 |
      |---|---|---|
      | 等距进制 | 恒定 | ❌ |
      | 二进制位序 | 跳跃 (指数) | ❌ (R106 覆盖窄) |
      | 素数参照 | 平滑递减 (对数) | ✅ 天然匹配 |

    ★ 机制:
      素数密度递减 = 素数定理 π(x) ~ x/ln x (对数分布)
      ANS 距离效应 (R101: 距离小精确): 素数间距 = 距离
      ⟹ 素数参照 = ANS 的天然实现

    ★ 零点重观测 (用户指令, next):
      用找到的零点 (R085: 0 = ±1 折叠类) 重新观测素数密度递减

    ★ 与既有 claim 的关系:
      - R101 (ANS 距离效应): 素数参照 = ANS 实现
      - R097 (素数幂链): 素数 = 数轴参照
      - R106 (注意力覆盖): 二进制窄, 素数匹配
      - R085 (零点折叠类): 零点重观测 (next)
      - R072 (声明): 素数参照的声明化
      结论: 素数参照天然匹配数感精度曲线 (对数递减 =
      小数精确大数模糊); 等距/二进制不匹配; 素数 = ANS 天然实现。

    ★ 支撑:
      - 素数定理: π(x) ~ x/ln x (对数分布)
      - 枚举: 素数间距区间统计
  formal_math: |
    -- 素数间距: gap ~ ln x (素数定理推论)
    -- 密度: π(x) ~ x/ln x (递减)
    -- 数感精度: ANS 距离效应 (距离小精确)
    -- 匹配: 素数间距 = ANS 距离

objects: []

definitions_used:
- R101 / R097 / R106 / R085 / R072

assumptions:
- "素数密度递减 = 对数分布 (素数定理)"
- "数感精度曲线 = ANS 距离效应"
- "素数参照天然匹配数感"

conclusion: "用户断言成立 (2026-08-12): 素数参照天然实现数感 — 素数间距平滑
  增长 (对数, π(x)~x/ln x) = 小数精确大数模糊; 等距/二进制不匹配;
  素数参照 = ANS 天然实现。零点重观测为 next。"

known_special_cases:
  - "小范围: 素数密 (间距 3.4) 精确"
  - "大范围: 素数疏 (间距 6.0) 模糊"
  - "等距进制: 精度恒定 (不匹配)"

counterexample_search:
  status: done
  search_space: "素数间距区间统计 (枚举)"
  smallest_checked_size: 500
  result: "间距平滑增长 ✓; 三参照对比 ✓; 无反例"

formalization:
  prover: Lean4
  file: ""
  theorem_name: ""
  status: not_started
  sorry_free: false

failure_modes:
  - "素数定理是渐近: 局部波动存在 (短区间间距不均匀)"
  - "匹配是曲线形状匹配: 非精确对应"
  - "零点重观测待做: 见 next_action"

evidence_for: []
evidence_against: []

next_action: "用零点 (R085) 重观测素数密度递减"

notes: "观点归属: 用户 (2026-08-12) 提出 (原话原封保存); assistant 执行机械验证
  + claim 落盘。"
