id: R053
title: "R053 (user-proposed 2026-08-12): the SAME axis with its basepoint moving along ANY other direction axis — lossless mapping and lossless compression (teleport bijection + move amount injective encoding)"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
zero_role:
  basepoint: true
  additive_identity: false
  bottom: false
  seed: false
branch: toolkit
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"
proposed_by: user (ethanw, 2026-08-12)

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。断言原文: "同一根轴, 基点在任意其他方向轴
       上移动, 无损映射、无损压缩。"

    ★ 断言内容:
      保持同一根轴 A (如发散轴 lift), 把基点从 0 移到 f — f 在任意其他方向轴
      w 上 (f = t·w, 如周期轴 J 或任意斜方向) — 轴本身的映射与压缩无损。

    ★ 机制 (全部为已证理论的直接应用):
      ① 基点移动 = 穿折越 teleport T(x) = x + (f - 0) = x + f (TK1:
         teleport e f x = (f -ᵥ e) +ᵥ x)
      ② T 双射 (逆 T⁻¹(x) = x - f) ⟹ 无损映射 — 移动后的轴与原轴同构
         (同结构, 新基点)
      ③ 移动量 Δ = f 单射编码 ⟹ 无损压缩 (R048)
      ④ 移动保轴结构: T(a₂) - T(a₁) = a₂ - a₁ (TK1: teleport 保位移)

    ★ 核心定理 (Lean 验收, BasepointMove.lean, 无 sorry):
      ① teleport_bijective: T(x) = x + f 双射 (逆 x - f) — 基点移动双向无损,
         移动后轴与原轴同构
      ② teleport_lossless: T 无损 — ∃ d, d (z + f) = z (R048 应用)
      ③ basepoint_move_preserves_structure: 移动保轴结构 — T(a₂) - T(a₁) =
         a₂ - a₁ (位移不变, 轴结构存活, 只换基点)
      ④ move_amount_lossless: 移动量 Δ 单射编码无损 (R048)
      ⑤ basepoint_move_lossless: 同一根轴基点沿任意方向轴移动无损 (核心)

    ★ 机械验证 (枚举=证据):
      1. 基点沿任意方向轴移动: teleport 可逆无损 (500 组): PASS
      2. 基点移动后相对位移保持, (基点,位移) 无损还原 (500 组): PASS
      3. 基点移动量单射编码无损: PASS
      4. 基点移动保轴结构 (差不变, 300 组): PASS

    ★ 与既有 claim 的关系:
      - TK1 (基点穿折越 teleport): 本 claim 的机制 — T 双射已证
      - R048 (单射 ⟹ 无损): 压缩机制
      - R047 (发散/周期轴对): 基点可移到周期轴 J 上 (w = J)
      - R050 (同方向迭代): 迭代差 = 基点移动的特例 (w 沿迭代方向)
      - R052 (同一基点任意方向轴): 本 claim 是"基点移动"版本 — 轴固定, 基点动
      结论: 轴固定基点动 = teleport 双射 (无损映射) + 移动量单射 (无损压缩)
      — 与 R052 (基点固定轴动) 对偶, 链条 R047-R053 全部闭合。

    ★ 支撑 (Lean):
      - teleport_bijective / teleport_lossless /
        basepoint_move_preserves_structure / move_amount_lossless /
        basepoint_move_lossless [BasepointMove.lean]
      - teleport [TK1 BasepointGen.lean]
      - injective_is_lossless [LosslessCompression.lean]
  formal_math: |
    -- teleport 双射 (基点移动可逆):
    theorem teleport_bijective (f : ComplexAxis) :
      Function.Bijective (fun z : ComplexAxis => z + f)
    -- 基点移动无损 (R048):
    theorem teleport_lossless (f : ComplexAxis) :
      ∃ d : ComplexAxis → ComplexAxis, ∀ z, d (z + f) = z
    -- 移动保轴结构 (位移不变):
    theorem basepoint_move_preserves_structure (f a₁ a₂ : ComplexAxis) :
      (a₂ + f) - (a₁ + f) = a₂ - a₁
    -- 移动量无损压缩:
    theorem move_amount_lossless {P : Type} [Nonempty ComplexAxis]
      (c : ComplexAxis → P) (hc : Function.Injective c) :
      ∃ d : P → ComplexAxis, ∀ f, d (c f) = f
    -- 核心: 同一根轴基点沿任意方向轴移动无损:
    theorem basepoint_move_lossless (f : ComplexAxis) :
      ∃ d : ComplexAxis → ComplexAxis, ∀ z, d (z + f) = z

objects:
- teleport_bijective
- teleport_lossless
- basepoint_move_preserves_structure
- move_amount_lossless
- basepoint_move_lossless

definitions_used:
- TK1 BasepointGen (teleport) / R048 LosslessCompression (injective_is_lossless) / C011 ComplexAxis

assumptions:
- "基点移动量 f 在任意其他方向轴 w 上 (f = t·w)"
- "同一根轴 A 保持 (轴结构不变, 只换基点)"
- "无损 = 双射/单射 (R048)"

conclusion: "用户断言成立 (2026-08-12): 同一根轴, 基点在任意其他方向轴上移动,
  无损映射 (teleport 双射, 轴与原轴同构) + 无损压缩 (移动量单射编码, R048)
  — 轴固定基点动与 R052 基点固定轴动对偶, 链条闭合。"

known_special_cases:
  - "基点移到周期轴 J 上 (w = J, R047): 发散轴基点周期化, 无损"
  - "基点沿迭代方向移动 (R050): 迭代差 = 基点移动特例"
  - "移动保结构: T(a₂) - T(a₁) = a₂ - a₁ (位移不变)"

counterexample_search:
  status: done
  search_space: "任意方向轴移动随机枚举 (R053_basepoint_move.py: teleport 可逆/位移保持/移动量编码/结构保持 4 组)"
  smallest_checked_size: 500
  result: "4/4 PASS; 无反例"

formalization:
  prover: Lean4
  file: formal/Formal/Toolkit/BasepointMove.lean
  theorem_name: teleport_bijective, teleport_lossless, basepoint_move_preserves_structure, move_amount_lossless, basepoint_move_lossless
  status: done
  sorry_free: true

failure_modes:
  - "基点移动 = 平移 (加法): 需要加法群结构; 非仿射结构需另论"
  - "无损映射 ≠ 结构不变量: 位移不变 (差), 但绝对位置变 (基点语义)"
  - "移动量编码需要单射 (R048): 碰撞编码有损"

evidence_for: []
evidence_against: []

next_action: "基点移动无损接入六集群神经层 (编排集群 = 基点对齐, RulerNeural): 每模态基点可沿任意方向轴移动而无损; 与 RulerProphecy (相位截断) 组合"

notes: "观点归属: 用户 (2026-08-12) 提出; 机械验证 + Lean 形式化为本 session 执行。"
