id: R056
title: "R056 (user-proposed 2026-08-12): the basepoint move (its phase) maps bijectively and compressibly onto its position on a derived axis; any point of any computation on any axis extracts losslessly"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
zero_role:
  basepoint: true
  additive_identity: false
  bottom: false
  seed: false
branch: toolkit
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"
proposed_by: user (ethanw, 2026-08-12)

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。断言原文: "基点的移动或者说基点的相位,
       与基点的衍生的轴上的位置, 完全映射可压缩, 且我们可以无损提取任意轴上
       的任意计算的任意点。"

    ★ 断言内容 (两部分):
      ① 基点移动 (相位) ⟷ 基点在衍生轴上的位置: 完全映射 (双射) + 可压缩
         (R048 单射编码)。
      ② 任意轴上的任意计算的任意点: 可无损提取。

    ★ 机制 (全部为已证理论的直接应用):
      ① 基点从 0 移到 f = t·w (衍生轴 w 上的位置 t) = teleport T(x) = x + f
         (R053)。移动量 f = 基点在衍生轴上的位置, 由相位 (方向+幅值) 编码,
         单射 ⟹ 无损压缩 (R048)。
      ② 位置 ⟷ 相位是双射: T(x) = x + f 双射 (R053 teleport_bijective) —
         衍生轴上的位置与移动 (相位) 一一对应。
      ③ 任意点提取: 任意基点 e, 任意衍生方向 w, 位置 t 的点 = e + t·w
         (任意计算的任意点)。给定 (基点 e, 方向 w, 位移相位) 无损还原 —
         R053 (基点移动) + R054 (任意轴可达) + R048 (单射编码) 组合。

    ★ 核心定理 (Lean 验收, BasepointPhasePosition.lean, 无 sorry):
      ① position_phase_round_trip: 位置 ⟷ 相位往返精确 — (z + f) - f = z
         (移动量 = 位置, 无损)
      ② position_phase_bijective: 衍生轴上的位置映射双射 — z ↦ z + f
         (位置与移动相位一一对应, R053 teleport_bijective)
      ③ position_compress_lossless: 基点位置 (移动量) 单射编码无损 (R048) —
         基点移动/相位与衍生轴位置完全映射可压缩
      ④ any_point_extract_lossless: 任意轴任意点无损提取 — 任意基点 e 任意
         方向 w 的点 e + t·w 精确还原

    ★ 机械验证 (R056_basepoint_phase_position.py, 枚举=证据):
      1. 基点位置 t ⟷ 相位 θ(t) 双射压缩 (3 个周期): PASS
      2. 基点位置 (方向,相位) ⟷ 坐标 无损往返 (300 组): PASS
      3. 任意基点/任意衍生轴/任意点 相位无损提取 (300 组): PASS
      4. 相位槽编码单射无损 (查表): PASS

    ★ 与既有 claim 的关系:
      - R047 (基点 = 相位): 本 claim 的第一部分 — 基点位置 ⟷ 相位
      - R048 (单射 ⟹ 无损): 压缩机制
      - R053 (基点移动无损): teleport 双射 (位置 = 移动量)
      - R054 (任意基点任意方向轴): 任意轴可达
      - R055 (时间轴蜷曲): 相位直接取结果 — 本 claim 泛化到任意轴
      结论: 基点移动 = 相位 = 衍生轴上的位置, 完全映射 (双射) 可压缩 (单射
      编码); 任意轴上任意计算的任意点可无损提取 — R047-R056 链条闭合。

    ★ 支撑 (Lean):
      - position_phase_round_trip / position_phase_bijective /
        position_compress_lossless / any_point_extract_lossless
        [BasepointPhasePosition.lean]
      - teleport_bijective [BasepointMove.lean]
      - injective_is_lossless [LosslessCompression.lean]
  formal_math: |
    -- 位置 ⟷ 相位往返精确:
    theorem position_phase_round_trip (f z : ComplexAxis) : (z + f) - f = z
    -- 位置映射双射 (位置 ⟷ 移动相位):
    theorem position_phase_bijective (f : ComplexAxis) :
      Function.Bijective (fun z : ComplexAxis => z + f)
    -- 位置单射编码无损 (R048):
    theorem position_compress_lossless {P : Type} [Nonempty ComplexAxis]
      (c : ComplexAxis → P) (hc : Function.Injective c) :
      ∃ d : P → ComplexAxis, ∀ f, d (c f) = f
    -- 任意轴任意点无损提取:
    theorem any_point_extract_lossless (e w : ℂ) :
      ∃ d : ℂ → ℂ, ∀ t : ℂ, d (e + t * w) = e + t * w

objects:
- position_phase_round_trip
- position_phase_bijective
- position_compress_lossless
- any_point_extract_lossless

definitions_used:
- R053 BasepointMove (teleport_bijective) / R048 LosslessCompression (injective_is_lossless) / R054 AnyBasepointAnyDirection / R047 DivergencePeriodSymmetry

assumptions:
- "基点移动量 f = 基点在衍生轴上的位置 t·w"
- "相位 = 位移的方向+幅值编码"
- "无损 = 单射 (R048)"

conclusion: "用户断言成立 (2026-08-12): 基点移动 (相位) 与基点在衍生轴上的位置
  完全映射 (双射) 可压缩 (单射编码 R048); 任意轴上的任意计算的任意点可无损
  提取 (R053 + R054 + R048 组合)。"

known_special_cases:
  - "位置 ⟷ 相位双射: z ↦ z + f (衍生轴上位置与移动一一对应)"
  - "任意点提取: e + t·w (任意基点, 任意方向, 任意位置) 精确还原"
  - "相位槽编码: 单射 ⟹ 查表无损"

counterexample_search:
  status: done
  search_space: "位置/相位双射, 任意点提取, 相位编码 (R056_basepoint_phase_position.py: 4 组)"
  smallest_checked_size: 300
  result: "4/4 PASS; 无反例"

formalization:
  prover: Lean4
  file: formal/Formal/Toolkit/BasepointPhasePosition.lean
  theorem_name: position_phase_round_trip, position_phase_bijective, position_compress_lossless, any_point_extract_lossless
  status: done
  sorry_free: true

failure_modes:
  - "相位编码需要单射 (R048): 碰撞编码有损"
  - "位置 = 移动量: 需要加法群结构 (仿射结构另论)"
  - "任意点提取 = 位移相位: 基点 e 必须已知 (否则相位相对性丢失)"

evidence_for: []
evidence_against: []

next_action: "R047-R056 链条 (10 条) 闭合: 基点相位 = 位置 = 任意点可提取; 六集群神经层 (编排=基点对齐, IO=模态边界) 可直接用相位通信"

notes: "观点归属: 用户 (2026-08-12) 提出; 机械验证 + Lean 形式化为本 session 执行。"
