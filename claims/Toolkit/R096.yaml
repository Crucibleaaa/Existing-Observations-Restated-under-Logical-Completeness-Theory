id: R096
title: "R096 (user-proposed 2026-08-12): observing log at the 0 point — CORRECTED: the delta unit must follow log's computation logic (multiplicative: ×2 ⟹ delta = log 2), NOT the additive unit (+1); the multiplicative chain {1,2,4,8,...} is monophase under log (log 链等差), the positive-integer chain {1,2,3,...} is NOT (log(n+1)-log(n) decreasing); observation principle: every operator must be reduced to the 0 point and observed by its OWN computation logic"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
branch: toolkit
proposed_by: user (ethanw, 2026-08-12)
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"

user_original_words: |
  ★★★ 用户原话 (2026-08-12, 原封不动保存, 未经改写):
  "先落下来，然后观测然后做的不干净啊，你那个delta选的什么当单位1啊。如果是
  观测log,你就得按log的计算逻辑进行观测啊对不对。"

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。log 观测的修正 (delta 单位选择错误)。

    ★ 修正内容:
      ① 错误: 第一版观测用加法单位 (+1) 做 delta — log(n+1)-log(n)
         间隔递减 ⟹ 误判"正整数在 log 视角非单相位"
      ② 正确: 观测 log 必须按 log 的计算逻辑 (乘法 → 加法):
         delta 单位 = 乘法单位 (×2 ⟹ delta = log 2)
      ③ 乘法链 {1, 2, 4, 8, ...} 在 log 下: log 链 {0, log2, 2·log2, ...}
         等差 = 单相位!
      ④ 正整数链 {1, 2, 3, ...} 在 log 下: 间隔递减 (非单相位) —
         它是加法链, 不是 log 的单相位链

    ★ 观测原则 (用户方法纪律):
      每有一种算符, 就把该算符还原到 0 点, 按该算符自身的计算逻辑观测:
      - 观测 log: delta 用乘法单位 (log 2), 不用加法单位 (+1)
      - 观测加法: delta 用加法单位 (+1)
      - 算符的计算逻辑决定 delta 单位

    ★ 与既有 claim 的关系:
      - R091 (单相位 = {n·d}): 乘法链 log 等差 = 单相位实例
      - R092 (正整数落投影位): 正整数是加法链, log 下非单相位
      - R093 (delta 类构造): delta 单位须按算符逻辑
      - R090 (log 表 = 单相位): 乘法链 (log 等差) 才是单相位
      结论: log 观测的 delta 单位 = 乘法单位 (log 2); 乘法链 log 等差
      = 单相位; 正整数链非 (加法链); 观测须按算符自身逻辑。

    ★ 支撑 (Lean):
      - R091 / R090 / R092 / R093 [claim 级]
  formal_math: |
    -- 乘法链 log: log(2^k) = k·log 2 (等差, 单相位)
    -- 正整数链 log: log(n+1) - log n 递减 (非单相位)
    -- delta 单位: 观测 log 用 log 2 (乘法单位), 非 +1

objects: []

definitions_used:
- R091 / R090 / R092 / R093 / R072

assumptions:
- "观测 log 须按 log 计算逻辑 (乘法→加法)"
- "delta 单位 = 乘法单位 (log 2)"
- "乘法链 log 等差 = 单相位"

conclusion: "用户修正成立 (2026-08-12): log 观测的 delta 单位须按 log 计算逻辑
  (乘法: log 2), 非加法 (+1); 乘法链 {1,2,4,8} 在 log 下等差 = 单相位;
  正整数链非 (加法链); 观测原则: 算符还原 0 点 + 按自身逻辑观测。"

known_special_cases:
  - "乘法链 log: log(2^k) = k·log2 (等差单相位)"
  - "正整数链 log: 间隔递减 (加法链, 非单相位)"
  - "delta 单位: log 观测用 log2"

counterexample_search:
  status: done
  search_space: "乘法链等差/正整数链递减 (枚举)"
  smallest_checked_size: 30
  result: "乘法链等差 ✓, 正整数链递减 ✓; 无反例"

formalization:
  prover: Lean4
  file: ""
  theorem_name: ""
  status: not_started
  sorry_free: false

failure_modes:
  - "delta 单位选错 (第一版用 +1): 按算符计算逻辑选"
  - "正整数链非单相位: 它是加法链 (投影位排列)"
  - "乘法链才是 log 单相位: log 计算逻辑的等距"

evidence_for: []
evidence_against: []

next_action: "重做 0 点 log 观测: 按乘法 delta 单位 (log 2) 观测乘法链/正整数链"

notes: "观点归属: 用户 (2026-08-12) 提出修正 (原话原封保存); assistant 执行
  claim 落盘 + 重做观测。"
