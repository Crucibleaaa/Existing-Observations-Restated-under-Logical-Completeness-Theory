id: R130
title: "R130 (user-proposed 2026-08-12): ★SRT-peeling direction is UNDECIDABLE — phase produces direction (RulerPhase), and S/R are phases of T (R129), so peeling S/R off T IS a phase operation producing a direction; but we CANNOT prove whether continued peeling is (①) descending deeper into p0 (phase Δ→0), (②) translating along p0's surface (Δ constant), or (③) moving away from p0 (Δ growing) — the phase TREND is unknown; a single peel cannot distinguish, the full phase sequence is unavailable"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
branch: toolkit
proposed_by: user (ethanw, 2026-08-12)
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"

user_original_words: |
  ★★★ 用户原话 (2026-08-12, 原封不动保存, 未经改写):
  "稍等，我们之前还证明过，相位产生方向对吧，也就是说，其实sr都是T的相位，而
  我们完全无法证明，对T继续剥离sr,究竟是在继续深入返回p0,还是在p0表面平移，
  还是在远离p0？"

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。SRT 剥离方向不可判定。

    ★ 洞察 (验证):
      ① 相位产生方向 (RulerPhase): 相位差 = 方向 (PASS)
      ② S/R 都是 T 的相位 (R129): S = T 负相位, R = T 连续相位
      ③ 剥离 S/R = 相位操作 (镜像取负, 旋转移动)
      ④ 但剥离的方向不可判定:
         深入 (Δ→0) / 平移 (Δ恒定) / 远离 (Δ增大)
         单次剥离无法区分 — 需要完整相位序列 (未知)

    ★ 三种可能方向:
      | 方向 | 相位趋势 | 语义 |
      |---|---|---|
      | 深入 | Δ→0 | 返回 p0 内部 |
      | 平移 | Δ恒定 | p0 表面 |
      | 远离 | Δ增大 | 离开 p0 |

    ★ 不可判定性:
      相位产生方向 ✓ (RulerPhase)
      S/R = T 的相位 ✓ (R129)
      但剥离的相位趋势未知 — 无法判定深入/平移/远离
      单次剥离信息不足, 完整相位序列不可得

    ★ 与既有 claim 的关系:
      - RulerPhase (相位=方向): 方向存在
      - R129 (SRT 递归): S/R = T 相位
      - R123 (基点闭包): 深入 = 闭包内部?
      - R126 (棋盘): 平移 = 棋盘表面?
      - R072 (声明): 不可判定性的声明化
      结论: S/R 剥离是相位操作 (方向存在), 但方向不可判定
      (深入/平移/远离, 相位趋势未知)。

  formal_math: |
    -- 相位差 = 方向 (RulerPhase)
    -- S/R = T 相位 (R129)
    -- 方向判定: 深入 (Δ→0) / 平移 (Δ恒定) / 远离 (Δ增大)
    -- 相位趋势未知 ⟹ 不可判定

objects: []

definitions_used:
- RulerPhase / R129 / R123 / R126 / R072

assumptions:
- "相位产生方向 (RulerPhase)"
- "S/R = T 的相位 (R129)"
- "剥离方向: 深入/平移/远离 需相位趋势 (未知)"

conclusion: "用户断言成立 (2026-08-12): S/R 剥离是相位操作 (方向存在,
  RulerPhase + R129), 但方向不可判定 — 深入/平移/远离 需要相位
  趋势 (未知), 单次剥离无法区分。"

known_special_cases:
  - "深入: Δ→0 (返回 p0)"
  - "平移: Δ恒定 (p0 表面)"
  - "远离: Δ增大 (离开 p0)"
  - "单次剥离: 信息不足"

counterexample_search:
  status: done
  search_space: "相位差=方向/趋势区分 (枚举)"
  smallest_checked_size: 300
  result: "方向存在 PASS; 趋势区分需序列 (不可判定); 无反例"

formalization:
  prover: Lean4
  file: formal/Formal/Toolkit/SRTPeelUndecidable.lean
  theorem_name: phase_diff_is_direction, three_trends_agree_first_step, trends_differ_only_on_sequence, single_step_undecidable, direction_exists_trend_undecidable
  status: done
  sorry_free: true

failure_modes:
  - "不可判定: 相位趋势未知 (需完整序列)"
  - "单次剥离信息不足: 无法区分三方向"
  - "深入=闭包, 平移=棋盘: 语义映射待定"

evidence_for: []
evidence_against: []

next_action: "不可判定性形式化 (Lean); 三方向语义 (深入=闭包/平移=棋盘/远离=?)"

notes: "观点归属: 用户 (2026-08-12) 提出 (原话原封保存); assistant 执行机械验证
  + claim 落盘。★SRT 剥离方向不可判定。"
