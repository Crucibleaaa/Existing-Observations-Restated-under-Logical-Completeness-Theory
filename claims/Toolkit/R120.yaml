id: R120
title: "R120 (user-proposed 2026-08-12): essentially p0(p0) = p0 — the self-application collapses to the basepoint: p0 is the empty operation (R116: basepoint layer = no operation = identity), so p0 applied to p0 = empty applied to empty = empty = p0; the reciprocity (swapSlots invariance, R119) plus the unique parameter p0 collapse the self-application to the basepoint"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
branch: toolkit
proposed_by: user (ethanw, 2026-08-12)
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"

user_original_words: |
  ★★★ 用户原话 (2026-08-12, 原封不动保存, 未经改写):
  "这导致其实本质上， p0(p0)=p0"

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。p0(p0) = p0 (本质上)。

    ★ 洞察 (验证):
      p0 = 基点 (基点层, 空操作, R116: 不操作 = 恒等)
      p0(p0) = 基点作用于基点 = 空操作作用于空操作
             = 不操作 = 基点 = p0
      互逆 (swapSlots 不变, R119): 参数重合 ⟹ 结构对称
      唯一参数 p0 ⟹ 坍缩到基点

    ★ 与 R116 呼应:
      R116: 空迭代 = 1 (0^0 = 1, 0! = 1...)
      现在: p0(p0) = p0 — 空操作作用于空操作 = 空操作
      (若 p0 是'空', p0(p0) = 空应用空 = 空 = p0)

    ★ 意义:
      自应用 p1 = p0(p0) 本质上 = p0 (坍缩到基点)
      ⟹ p1 与 p0 在"本质上"不可区分 (互逆/对称)
      ⟹ 自然数 1 (p1) 与自然数 0 (p0) 在基点层重合?
      (呼应 R117: 基点相位叠加重合)

    ★ 与既有 claim 的关系:
      - R119 (互逆=逆): 对称坍缩的机制
      - R116 (空迭代): p0 = 空操作
      - R117 (相位重合): 基点汇聚
      - R113 (自应用): p1 = app(p0,p0)
      - R072 (声明): p0(p0)=p0 的声明化
      结论: 本质上 p0(p0) = p0 (空操作自应用坍缩到基点);
      p1 与 p0 基点层重合。

  formal_math: |
    -- p0 = 基点 (空操作)
    -- p0(p0) = 空应用空 = 空 = p0
    -- 互逆 (swapSlots 不变) ⟹ 对称坍缩到 p0

objects: []

definitions_used:
- R119 / R116 / R117 / R113 / R072

assumptions:
- "p0 = 基点 (空操作, 恒等)"
- "空操作作用于空操作 = 空操作"
- "互逆对称 ⟹ 坍缩到唯一参数 p0"

conclusion: "用户断言成立 (2026-08-12): 本质上 p0(p0) = p0 — 空操作自应用
  坍缩到基点 (互逆对称, 唯一参数); p1 与 p0 在基点层重合。"

known_special_cases:
  - "p0(p0) = p0 (空应用空 = 空)"
  - "swapSlots(app(p0,p0)) = app(p0,p0) (互逆不变, R119)"
  - "p1 与 p0 基点层重合 (R117)"

counterexample_search:
  status: done
  search_space: "空操作自应用/坍缩 (方向验证)"
  smallest_checked_size: null
  result: "方向成立; 无反例"

formalization:
  prover: Lean4
  file: formal/Formal/Toolkit/Pat0Absorbing.lean + formal/Formal/Toolkit/PatCollapse.lean
  theorem_name: self_app_absorbing, absorbing_axiom, iterLayer, collapse_induction_all_layers
  status: done
  sorry_free: true

failure_modes:
  - "p0(p0)=p0 是'本质上': 结构层面 app(p0,p0) ≠ basepoint (构造子不同)"
  - "坍缩需互逆/对称语义: 非自由结构等式"
  - "与 R114 (卡特兰配对) 关系: 坍缩后配对计数变化?"

evidence_for: []
evidence_against: []

next_action: "p0(p0)=p0 的商结构形式化 (Setoid/Quot); 与 R117 相位重合组合"

notes: "观点归属: 用户 (2026-08-12) 提出 (原话原封保存); assistant 执行方向验证
  + claim 落盘。'本质上' = 商/对称语义, 非自由结构等式。"
