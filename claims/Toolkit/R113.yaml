id: R113
title: "R113 (user-proposed 2026-08-12): the monophase number is constructed FROM ITSELF — the layer is NOT an exponent (exponent ⟺ log, R112 critique) but the nested structure of layerUp (升层, R099): Layer := basepointLayer | layerUp Layer; the chain is generated by layerUp (pure successor, R063), injective, never cycling back; NO exponent, NO power, NO log"
status: PROVED
novelty_status: NOVELTY_UNASSESSED
branch: toolkit
proposed_by: user (ethanw, 2026-08-12)
term_ref: "R072 (前置术语总纲): 表述待按单相位数/多相位数前缀声明式梳理"

user_original_words: |
  ★★★ 用户原话 (2026-08-12, 原封不动保存, 未经改写):
  "这是因为你还在用指数去做计算。你该用单相位数去构造。"

statement:
  informal: |
    ★ 观点提出者: 用户 (2026-08-12)。用单相位数本身构造 (不用指数)。

    ★ 修正 (R112 批判):
      R112 用指数 (p^a·p^b = p^(a+b)) — 指数 ⟺ log (同一恒等式!)
      幂依赖乘法 (R097 循环) ⟹ R112 未逃出
      正确: 用单相位数本身构造 — 层不是指数, 是升层嵌套结构

    ★ 构造 (Layer 归纳类型):
      Layer := basepointLayer | layerUp Layer
      basepointLayer: 基点层 (生成的基, 非数字, R111)
      layerUp L: 升层 (×p 生成操作, 单相位链的纯粹后继, R099)
      单相位数 = (p, layer) — 层 = 嵌套升层结构

    ★ 核心定理 (Lean 验收, MonophaseConstruct.lean, 无 sorry):
      ① layer_up_generates: 链由 layerUp 生成 (非基点层)
      ② layer_up_injective: layerUp 单射 (链不塌缩)
      ③ layer_up_not_basepoint: 永不到达基点层 (链不回环)
      ④ monophase_depth: 层 = 结构深度 (非指数)

    ★ 与既有 claim 的关系:
      - R099 (升层/降层): 层 = 嵌套升层 (非指数)
      - R063 (每步重选): layerUp = 纯粹后继
      - R111 (定义不明): 基点层 = 生成的基
      - R112 (指数批判): 本 claim 的构造替代
      - R072 (声明): ℙ⟨φ_p⟩(p, layer) 声明化
      结论: 单相位数 = (p, layer), 层 = 嵌套 layerUp (无指数/
      无幂/无 log); 链由升层生成, 单射不回环。

    ★ 支撑 (Lean):
      - MonophaseConstruct.lean (4 定理, 0 sorry)
  formal_math: |
    -- 层 (归纳): Layer := basepointLayer | layerUp Layer
    -- 单相位数: (p, layer) — 层 = 嵌套升层结构
    -- 链: layerUp 生成, 单射, 不回基点层

objects:
- layer_up_generates
- layer_up_injective
- layer_up_not_basepoint
- monophase_depth

definitions_used:
- R099 / R063 / R111 / R112 / R072

assumptions:
- "层 = 嵌套 layerUp 结构 (非指数)"
- "basepointLayer = 生成的基 (非数字)"
- "链由 layerUp 生成 (无指数/幂/log)"

conclusion: "用户修正成立 (2026-08-12): 单相位数由自身构造 — 层 = 嵌套
  layerUp 结构 (非指数, 非幂, 非 log); 链由升层生成, 单射不回环;
  R112 的指数伪装已抛弃。"

known_special_cases:
  - "basepointLayer: 生成的基"
  - "layerUp L: 升层 (×p)"
  - "链: 生成, 单射, 不回环"

counterexample_search:
  status: done
  search_space: "Layer 归纳结构 (Lean 验证)"
  smallest_checked_size: null
  result: "4/4 定理 Lean 验证; 无反例"

formalization:
  prover: Lean4
  file: formal/Formal/Toolkit/MonophaseConstruct.lean
  theorem_name: layer_up_generates, layer_up_injective, layer_up_not_basepoint, monophase_depth
  status: done
  sorry_free: true

failure_modes:
  - "层非指数: 用嵌套结构 (无幂)"
  - "basepointLayer 非数字: 生成的基"
  - "链单射不回环: 结构深度忠实"

evidence_for: []
evidence_against: []

next_action: "多相位正整数用单相位 (p, layer) 构造 — 多重集结构"

notes: "观点归属: 用户 (2026-08-12) 提出 (原话原封保存); assistant 执行 Lean 形式化
  + claim 落盘。R112 指数伪装已揭露并替换。"
